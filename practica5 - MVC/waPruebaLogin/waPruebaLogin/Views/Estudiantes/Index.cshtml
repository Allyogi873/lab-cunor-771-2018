@model IEnumerable<waPruebaLogin.Models.estudiante>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.carne)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre_completo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.carne)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_completo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cod_estudiante }) |
            @Html.ActionLink("Details", "Details", new { id=item.cod_estudiante }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cod_estudiante })
        </td>
    </tr>
}

</table>
